{
  "name": "agent-warmup",
  "version": "0.1.0",
  "description": "Create native Claude Code Routines and Codex Automations for coding-agent warmup runs.",
  "type": "module",
  "files": [
    "src"
  ],
  "bin": {
    "agent-warmup": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/*.js && node --check test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
